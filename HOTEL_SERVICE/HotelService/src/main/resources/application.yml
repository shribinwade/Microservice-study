server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/microservice
    username: postgres
    password: root
  application:
    name: Hotel-Service
  config:
    import: configserver:http://localhost:8085


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
